import { createClient } from "https://esm.sh/@supabase/supabase-js@2";

const propertyId = "524798829";

// get access token from env... actually let's just use the Supabase edge function but print the raw error if it occurs.
