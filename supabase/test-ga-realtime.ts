import { createClient } from "https://esm.sh/@supabase/supabase-js@2";

async function run() {
  const propertyId = "524798829";
  const accessToken = Deno.env.get("SUPABASE_ACCESS_TOKEN"); // wait, no, I need the google token
}
